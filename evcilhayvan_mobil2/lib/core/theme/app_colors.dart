import 'package:flutter/material.dart';

// ─── Plant Shop Yeşil Paleti ─────────────────────────────────────────────────

/// Ana marka rengi — koyu yeşil
const Color kPrimaryGreen = Color(0xFF2D6A4F);

/// Vurgu / buton rengi — orta yeşil
const Color kPrimaryLight = Color(0xFF52B788);

/// AppBar / header için koyu yeşil
const Color kPrimaryDark = Color(0xFF1B4332);

/// Açık arka plan / chip arka planı
const Color kPrimaryPale = Color(0xFFD8F3DC);

/// Orta ton yeşil
const Color kPrimaryMid = Color(0xFF40916C);

/// Secondary accent
const Color kAccentGreen = Color(0xFF74C69D);

/// Kart yüzeyi
const Color kSurface = Color(0xFFFFFFFF);

/// Primary üstündeki metin rengi
const Color kOnPrimary = Color(0xFFFFFFFF);

/// Açık yeşil üstündeki metin rengi
const Color kOnPrimaryLight = Color(0xFF1B4332);

/// Hata rengi (değişmez)
const Color kErrorRed = Color(0xFFE53935);

/// Yıldız / rating rengi
const Color kStarYellow = Color(0xFFFFC107);
